<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contacto</title>
    <%- include ('partials/head')%>
    <!-- Agregamos un enlace al archivo de estilos CSS para mejorar la apariencia del formulario -->
    <link rel="stylesheet" href="/css/styles.css"> 
</head>
<body>
    <%- include('partials/navigation') %>
    <div class="container vh-100 d-flex justify-content-center align-items-center">
        <div class="card bg-dark text-white">
            <div class="card-header">
                <!-- Añadimos un título al formulario -->
                <h2>Contacto</h2>
            </div>
            <div class="card-body">
                <form >
                    <!-- Añadimos etiquetas <label> correctamente -->
                    <div class="form-group">
                        <label for="email">Correo Electrónico:</label>
                        <input type="email" id="email" name="email" class="form-control" placeholder="Ingrese su correo electrónico" required>
                    </div>

                    <div class="form-group">
                        <label for="message">Mensaje:</label>
                        <!-- Cambiamos 'name' a 'id' para que coincida con el 'for' del label -->
                        <textarea id="message" name="message" rows="5" class="form-control" placeholder="Escriba su mensaje aquí" required></textarea>
                    </div>

                    <!-- Agregamos clases de Bootstrap al botón -->
                    <button type="submit" class="btn btn-primary">Enviar</button>
                </form>
            </div>
        </div>
    </div>
    <%- include('partials/footer') %>
</body>
</html>
